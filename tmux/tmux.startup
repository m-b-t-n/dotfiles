# Load Initial Window Settings

if-shell "uname | grep -q Linux" "source-file ~/dotfiles/tmux/linux/tmux.window.initial"
if-shell "hostname | grep -q Mac-mini" "source-file ~/dotfiles/tmux/macos/tmux.window.initial.macmini"

